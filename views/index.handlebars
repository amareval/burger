<h1>Eat Da Burger!</h1>

<h3>Eat that shit dog!</h3>

<ul>
  {{#each burgers}}
  	<li>
  	   <p>ID: {{this.id}}</p>
       <p>Wish: {{this.burger_name}}</p>
  	</li>
  {{/each}}
</ul>

<form action="/" method="POST">
	<textarea type="text" name="burger" class="align-center"></textarea>

	<button type="submit">Submit</button>
</form>
